@import url('https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,600;8..60,700&family=Inter:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap');

:root{
  --ink:#1b1f2a;
  --paper:#f6f4ef;
  --navy:#12172b;
  --navy-2:#1c2340;
  --navy-3:#242c50;
  --amber:#d8a24a;
  --teal:#4c7a79;
  --rose:#c15b4f;
  --green:#5fae74;
  --line: rgba(27,31,42,0.12);
  --line-dark: rgba(246,244,239,0.14);
  --radius: 6px;
}
*{ box-sizing:border-box; }
html,body,#root{ height:100%; width:100%; margin:0; padding:0; overflow:hidden; max-width:none; }
body{
  background:var(--navy);
  color:var(--paper);
  font-family:'Inter', sans-serif;
  -webkit-font-smoothing:antialiased;
  display:block;
  place-items:unset;
}
#root{ text-align:left; }
.app{
  display:grid;
  grid-template-columns: 272px 1fr 300px;
  width:100%;
  height:100vh;
  overflow:hidden;
}
@media (max-width: 980px){
  .app{ grid-template-columns: 1fr; grid-template-rows: auto 1fr auto; height:100vh; }
  .sidebar, .citations{ max-height:none; }
}

h1,h2,h3{ font-family:'Source Serif 4', serif; margin:0; }
button{ font-family:'Inter', sans-serif; }

::-webkit-scrollbar{ width:8px; height:8px; }
::-webkit-scrollbar-thumb{ background:rgba(246,244,239,0.15); border-radius:8px; }

/* ---------- left: source library ---------- */
.sidebar{
  background:var(--navy-2);
  border-right:1px solid var(--line-dark);
  display:flex;
  flex-direction:column;
  padding:26px 22px;
  height:100vh;
  overflow-y:auto;
}
.brand{ display:flex; align-items:baseline; gap:8px; margin-bottom:6px; }
.brand h1{ font-size:23px; color:var(--paper); font-weight:700; letter-spacing:-0.01em; }
.brand .mark{ color:var(--amber); }
.tagline{ font-size:12.5px; color:rgba(246,244,239,0.55); margin-bottom:26px; line-height:1.55; }

.upload-box{
  border:1.5px dashed rgba(216,162,74,0.4);
  border-radius:var(--radius);
  padding:20px 14px;
  text-align:center;
  cursor:pointer;
  transition:border-color .15s ease, background .15s ease, transform .1s ease;
  margin-bottom:4px;
  display:block;
}
.upload-box:hover{ border-color:var(--amber); background:rgba(216,162,74,0.05); }
.upload-box.dragover{ border-color:var(--amber); background:rgba(216,162,74,0.1); transform:scale(1.01); }
.upload-box.busy{ opacity:0.65; cursor:wait; }
.upload-box input{ display:none; }
.upload-icon{ font-size:22px; margin-bottom:6px; display:block; color:var(--amber); }
.upload-label{ font-size:13px; color:var(--paper); font-weight:600; display:block; }
.upload-sub{ font-size:11px; color:rgba(246,244,239,0.42); margin-top:3px; display:block; }

.upload-note{
  font-size:11.5px;
  line-height:1.5;
  border-radius:var(--radius);
  padding:8px 10px;
  margin:10px 0 20px;
}
.upload-note.ok{ background:rgba(95,174,116,0.12); color:#8fd1a3; border:1px solid rgba(95,174,116,0.25); }
.upload-note.error{ background:rgba(193,91,79,0.12); color:#e0958b; border:1px solid rgba(193,91,79,0.25); }

.section-label{
  font-family:'IBM Plex Mono', monospace;
  font-size:10.5px;
  letter-spacing:0.1em;
  text-transform:uppercase;
  color:rgba(246,244,239,0.4);
  margin-bottom:12px;
  display:flex;
  justify-content:space-between;
}
.doc-list{ display:flex; flex-direction:column; gap:8px; flex:1; }
.doc-card{
  border:1px solid var(--line-dark);
  border-radius:var(--radius);
  padding:10px 12px;
  font-size:12.5px;
  display:flex;
  gap:9px;
  align-items:flex-start;
  background:rgba(255,255,255,0.02);
}
.doc-dot{ width:6px; height:6px; border-radius:50%; background:var(--green); margin-top:5px; flex-shrink:0; }
.doc-card .name{ font-weight:500; color:var(--paper); word-break:break-word; margin-bottom:2px; }
.doc-card .meta{ font-family:'IBM Plex Mono', monospace; font-size:10px; color:rgba(246,244,239,0.4); }
.doc-empty{ font-size:12px; color:rgba(246,244,239,0.4); font-style:italic; line-height:1.5; }
.chunk-total{
  font-family:'IBM Plex Mono', monospace;
  font-size:10.5px;
  color:rgba(246,244,239,0.35);
  margin-top:6px;
  text-align:center;
}

.doc-list.compact .doc-card{ padding:7px 10px; }
.doc-list.compact .name{ font-size:12px; margin-bottom:0; }

.new-chat-btn{
  font-family:'IBM Plex Mono', monospace;
  font-size:10px;
  color:var(--amber);
  background:transparent;
  border:1px solid rgba(216,162,74,0.35);
  border-radius:20px;
  padding:2px 9px;
  cursor:pointer;
  transition:background .15s ease;
}
.new-chat-btn:hover{ background:rgba(216,162,74,0.1); }

.session-list{ display:flex; flex-direction:column; gap:4px; margin-bottom:8px; }
.session-item{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:6px;
  padding:8px 10px;
  border-radius:var(--radius);
  cursor:pointer;
  font-size:12.5px;
  color:rgba(246,244,239,0.6);
  transition:background .12s ease;
}
.session-item:hover{ background:rgba(255,255,255,0.05); }
.session-item.active{ background:rgba(216,162,74,0.12); color:var(--paper); }
.session-label{ overflow:hidden; text-overflow:ellipsis; white-space:nowrap; flex:1; }
.session-delete{
  background:transparent;
  border:none;
  color:rgba(246,244,239,0.35);
  font-size:15px;
  line-height:1;
  cursor:pointer;
  padding:0 2px;
  flex-shrink:0;
}
.session-delete:hover{ color:var(--rose); }

.api-status{
  margin-top:auto;
  padding-top:16px;
  border-top:1px solid var(--line-dark);
  font-family:'IBM Plex Mono', monospace;
  font-size:10.5px;
  color:rgba(246,244,239,0.45);
  display:flex;
  align-items:center;
  gap:7px;
}
.dot{ width:6px; height:6px; border-radius:50%; background:#6b7280; flex-shrink:0; }
.dot.ok{ background:var(--green); box-shadow:0 0 6px rgba(95,174,116,0.6); }
.dot.bad{ background:var(--rose); }

/* ---------- center: chat ---------- */
.chat-col{
  background:var(--paper);
  color:var(--ink);
  display:flex;
  flex-direction:column;
  min-width:0;
  height:100vh;
  overflow:hidden;
}
.chat-header{
  padding:22px 32px 16px;
  border-bottom:1px solid var(--line);
  display:flex;
  justify-content:space-between;
  align-items:center;
}
.chat-header h2{ font-size:19px; }
.header-right{ display:flex; align-items:center; gap:14px; }
.session-tag{ font-family:'IBM Plex Mono', monospace; font-size:10.5px; color:rgba(27,31,42,0.4); }
.ghost-btn{
  font-size:12px;
  font-weight:500;
  color:var(--teal);
  background:transparent;
  border:1px solid rgba(76,122,121,0.35);
  border-radius:20px;
  padding:5px 12px;
  cursor:pointer;
  transition:background .15s ease;
}
.ghost-btn:hover{ background:rgba(76,122,121,0.08); }

.messages{
  flex:1;
  min-height:0;
  overflow-y:auto;
  padding:28px 32px;
  display:flex;
  flex-direction:column;
  gap:22px;
}
.messages::-webkit-scrollbar-thumb{ background:rgba(27,31,42,0.15); }

.empty-state{ margin:auto; text-align:center; max-width:360px; color:rgba(27,31,42,0.45); }
.empty-state .glyph{ font-family:'Source Serif 4', serif; font-size:40px; color:var(--teal); margin-bottom:12px; }
.empty-state p{ font-size:13.5px; line-height:1.65; margin:6px 0 0; }

.msg{ display:flex; flex-direction:column; max-width:76%; }
.msg.user{ align-self:flex-end; align-items:flex-end; }
.msg.assistant{ align-self:flex-start; align-items:flex-start; }
.msg .who{
  font-family:'IBM Plex Mono', monospace;
  font-size:10px;
  letter-spacing:0.08em;
  text-transform:uppercase;
  color:rgba(27,31,42,0.4);
  margin-bottom:6px;
}
.bubble{ padding:13px 16px; border-radius:var(--radius); font-size:14.5px; line-height:1.65; }
.msg.user .bubble{ background:var(--navy); color:var(--paper); border-bottom-right-radius:2px; }
.msg.assistant .bubble{ background:#fff; border:1px solid var(--line); border-bottom-left-radius:2px; }
.bubble.uncertain{ background:#fdf8ee; border-color:rgba(216,162,74,0.4); color:#6b5218; }
.bubble.error{ background:#fdf1ef; border-color:rgba(193,91,79,0.35); color:#8c3a30; }

.rewrite-note{ font-family:'IBM Plex Mono', monospace; font-size:10.5px; color:var(--teal); margin-top:6px; padding-left:2px; }
.cite-strip{ margin-top:9px; display:flex; flex-wrap:wrap; gap:6px; }
.cite-tag{
  font-family:'IBM Plex Mono', monospace;
  font-size:10.5px;
  color:var(--teal);
  border:1px solid rgba(76,122,121,0.3);
  border-radius:20px;
  padding:3px 10px;
}
.cite-tag em{ font-style:normal; color:var(--amber); margin-left:2px; }

.figure-strip{ margin-top:10px; display:flex; flex-wrap:wrap; gap:8px; }
.figure-img{ max-width:280px; border:1px solid var(--line); border-radius:var(--radius); display:block; }

.thinking{ display:inline-flex; gap:4px; padding:2px 0; }
.dot-flash{
  width:6px; height:6px; border-radius:50%;
  background:rgba(27,31,42,0.3);
  animation: flash 1.1s infinite ease-in-out;
}
.dot-flash:nth-child(2){ animation-delay:0.15s; }
.dot-flash:nth-child(3){ animation-delay:0.3s; }
@keyframes flash{
  0%, 80%, 100%{ opacity:0.25; transform:translateY(0); }
  40%{ opacity:1; transform:translateY(-2px); }
}
@media (prefers-reduced-motion: reduce){
  .dot-flash{ animation:none; opacity:0.6; }
  .upload-box.dragover{ transform:none; }
}

.composer{ border-top:1px solid var(--line); padding:16px 32px 22px; display:flex; gap:10px; }
.composer input{
  flex:1;
  border:1px solid var(--line);
  border-radius:var(--radius);
  padding:12px 14px;
  font-family:'Inter', sans-serif;
  font-size:14px;
  background:#fff;
  color:var(--ink);
}
.composer input:disabled{ background:#f3f2ee; color:rgba(27,31,42,0.4); }
.composer input:focus{ outline:2px solid var(--amber); outline-offset:1px; }
.composer button{
  font-weight:600;
  font-size:13.5px;
  background:var(--navy);
  color:var(--paper);
  border:none;
  border-radius:var(--radius);
  padding:0 22px;
  cursor:pointer;
  transition:background .15s ease;
}
.composer button:hover:not(:disabled){ background:var(--navy-2); }
.composer button:disabled{ opacity:0.4; cursor:not-allowed; }

/* ---------- right: citation ledger ---------- */
.citations{
  background:var(--navy-2);
  border-left:1px solid var(--line-dark);
  padding:26px 20px;
  height:100vh;
  overflow-y:auto;
}
.citations h3{ font-size:15px; color:var(--paper); margin-bottom:4px; }
.citations .sub{ font-size:11.5px; color:rgba(246,244,239,0.45); margin-bottom:22px; line-height:1.5; }
.ledger-empty{ font-size:12px; color:rgba(246,244,239,0.38); font-style:italic; line-height:1.6; }
.ledger-item{
  display:flex;
  gap:10px;
  align-items:flex-start;
  border-bottom:1px solid var(--line-dark);
  padding:0 0 14px;
  margin-bottom:14px;
}
.ledger-item:last-child{ border-bottom:none; margin-bottom:0; }
.ledger-index{
  font-family:'IBM Plex Mono', monospace;
  font-size:10.5px;
  color:var(--amber);
  padding-top:1px;
  flex-shrink:0;
}
.ledger-item .file{ font-size:12.5px; color:var(--paper); font-weight:500; word-break:break-word; }
.ledger-item .page{ font-family:'IBM Plex Mono', monospace; font-size:11px; color:rgba(246,244,239,0.5); margin-top:2px; }